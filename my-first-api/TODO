1. Configure the Mysql statefulset to use a root password instead of leaving it blank
2. Set up helm chart for the full deployment rather than applying multiple YAMLs
3. Try to automate the csv column detection for table creation
4. Figure out why the API request.json is returning null when I attach json data.
